const SCREEN_WIDTH = 500;
const SCREEN_HEIGHT = 500;

function setup() {
    createCanvas(SCENE_WIDTH, SCENE_HEIGHT);
    noStroke();
    

}

function draw() {
    
}

function drawShape(x, y, innerShapes ) {
    
}

